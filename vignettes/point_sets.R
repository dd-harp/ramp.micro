## -----------------------------------------------------------------------------
library(ramp.micro)

## -----------------------------------------------------------------------------
x= c(1,0,1,0)
y= c(0,1,1,0)
xy0 <- cbind(x,y) 
xy0

## -----------------------------------------------------------------------------
xy1 <- unif_xy(50, 0, 10)
head(xy1, 4)

## -----------------------------------------------------------------------------
dim(xy1)

## ----fig.height=5, fig.width=5------------------------------------------------
par(mar = c(2,2,1,1), bty = "o")
plot(xy1)

## ----fig.height=5, fig.width=5------------------------------------------------
p2 <- lattice(7, 0, 10)
par(mar = c(2,2,1,1), bty = "o")
plot(p2)

## -----------------------------------------------------------------------------
dim(p2)

## -----------------------------------------------------------------------------
seeds <- unif_xy(23, 0, 50)
p3 <- clusters_xy(seeds, 8, 1)

## ----fig.height=5, fig.width=5------------------------------------------------
par(mar = c(2,2,1,1), bty = "o")
plot(p3)

