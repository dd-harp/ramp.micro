# Make a set of tiles that tesselate space

Make a set of tiles that tesselate space

## Usage

``` r
make_tiles(model)
```

## Arguments

- model:

  a model, defined as a compound \[list\]

## Value

the model, a compound \[list\]
