# Use communities to define patches

Use communities to define patches

## Usage

``` r
net2patches(model, i, cut = NULL)
```

## Arguments

- model:

  a ramp.micro model object

- i:

  which graph (see get_graph)

- cut:

  cutat argument, \# communities (optional)

## Value

a ramp.micro model object
