# Stretch (or shrink) the convex hull for plotting

Stretch (or shrink) the convex hull for plotting

## Usage

``` r
stretch_convex_hull(xy, fac)
```

## Arguments

- xy:

  a convex hull

- fac:

  a factor to stretch (\>1) or shrink (\<1) the hull for plotting

## Value

a new point set
